name: Generate Snake Animation

on:
    push:
    workflow_dispatch:
    schedule:
        - cron: "0 1 * * *"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: Platane/snk@v2
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist1/github-snake.svg
            dist1/github-snake-dark.svg?palette=github-dark
            dist1/ocean.gif?color_snake=orange&color_dots=#bfd6f6,#8dbdff,#64a1f4,#4b91f1,#3c7dd9

      # - name: Push to GitHub
      #   uses: EndBug/add-and-commit@v7.2.1
      #   with:
      #     branch: main
      #     message: 'Generate Contribution Snake'